[tool.poetry]
name = "test-python-constraint"
version = "0.1.0"
description = "Test fixture for OXDEV-67183 — Python version constraint blocks autofix"
authors = ["Yael <yael.bensaya@ox.security>"]

[tool.poetry.dependencies]
python = "^3.8"
Pillow = "9.5.0"
requests = "2.28.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
